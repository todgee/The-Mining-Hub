<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Schedule - Buddy</title>
    <link rel="stylesheet" href="/CSS/calendar.css" />
    <link rel="stylesheet" href="/CSS/navbar.css" />

    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css"
    />
    <script src="/JS/calendar.js"></script>
  </head>
  <body>
    <div class="calendar-container">
      <div class="event-feed-header">
        <h2>Schedule</h2>
        <button id="add-event-button">Add Event</button>
      </div>

      <div class="event-feed-container">
        <ul id="event-feed">
          <!-- Event items will be appended here -->
        </ul>
      </div>

      <div class="event-creation-container" id="event-creation-container">
        <h2>Create New Event</h2>
        <form id="event-form">
          <label for="event-type">Event Type:</label>
          <select id="event-type" name="event-type" required>
            <option value="" disabled selected>Select event type</option>
            <option value="transport">Transport</option>
            <option value="processing">Processing</option>
            <option value="drilling">Drilling</option>
          </select>
      
          <div id="transport-details-container" style="display: none;">
            <label for="transport-material">Transport Material:</label>
            <select id="transport-material" name="transport-material">
              <option value="" disabled selected>Select material</option>
              <option value="gold">Gold</option>
              <option value="copper">Copper</option>
            </select>
      
            <label for="transport-quantity">Quantity (tonnes):</label>
            <input type="number" id="transport-quantity" name="transport-quantity" min="0" step="0.1" placeholder="Enter quantity">
          </div>
      
          <label for="event-start-date">Start Date:</label>
          <input type="date" id="event-start-date" name="event-start-date" required />
      
          <label for="event-end-date">End Date:</label>
          <input type="date" id="event-end-date" name="event-end-date" required />
      
          <label for="event-time">Time:</label>
          <input type="time" id="event-time" name="event-time" required />
      
          <label for="event-description">Description:</label>
          <textarea
            id="event-description"
            name="event-description"
            placeholder="Write a brief description..."
            rows="3"
            required
          ></textarea>
      
          <button type="submit">Add Event</button>
        </form>
      </div>
      <%- include('partials/navbar') %>
    </div>
  </body>
</html>
